# Synchronization experiment 07

The retry window reduced transient failures on unstable connections. A follow-up run should isolate thumbnail
uploads from document changes so the queue-depth measurements are easier to interpret.
